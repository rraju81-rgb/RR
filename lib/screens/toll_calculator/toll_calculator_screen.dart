import 'package:flutter/material.dart';

class TollCalculatorScreen extends StatelessWidget {
  const TollCalculatorScreen({Key? key}) : super(key: key);
  @override
  Widget build(BuildContext context) => const Scaffold(body: Center(child: Text('Toll Calculator')));
}
