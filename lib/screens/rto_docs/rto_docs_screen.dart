import 'package:flutter/material.dart';

class RtoDocsScreen extends StatelessWidget {
  const RtoDocsScreen({Key? key}) : super(key: key);
  @override
  Widget build(BuildContext context) => const Scaffold(body: Center(child: Text('RTO Docs')));
}
