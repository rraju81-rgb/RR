import 'package:flutter/material.dart';

class WomenSafetyScreen extends StatelessWidget {
  const WomenSafetyScreen({Key? key}) : super(key: key);
  @override
  Widget build(BuildContext context) => const Scaffold(body: Center(child: Text('Women Safety')));
}
