import 'package:flutter/material.dart';

class FuelPricesScreen extends StatelessWidget {
  const FuelPricesScreen({Key? key}) : super(key: key);
  @override
  Widget build(BuildContext context) => const Scaffold(body: Center(child: Text('Fuel Prices')));
}
